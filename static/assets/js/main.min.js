var sv, ScriptsVari = {
    settings: {
        $body: $("body"),
        $html: $("html")
    },
    init: function() {
        sv = this.settings, this.bindUIActions(), this.onLoadActions(), this.scrollActions(), this.resizeActions()
    },
    onLoadActions: function() {
        $(window).load(function() {})
    },
    bindUIActions: function() {
        var i = $("#datetimepicker1").find("input").data("limit");
        $("#datetimepicker1").datetimepicker({
            format: "YYYY-MM-DD HH:mm:ss",
            stepping: 10,
            maxDate: i
        }), $("#map").length > 0 && (ScriptsVari.initMap(), ScriptsVari.initMap2())
    },
    scrollActions: function() {
        $(window).scroll(function() {})
    },
    resizeActions: function() {
        $(window).resize(function() {}).resize()
    }   
};
jQuery(document).ready(function(i) {
    ScriptsVari.init()
});